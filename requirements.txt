# AI-901 demo dependencies
# Pinned with `~=` (compatible release): picks up patch/minor fixes but stays
# inside the current major version, so a new major can't silently break a class.
# Pins are known-good as of mid-2026; Foundry SDKs move fast — bump if needed.

python-dotenv~=1.0

# Auth
azure-identity~=1.17

# Foundry / model access (2.x exposes an OpenAI-compatible client via get_openai_client)
azure-ai-projects~=2.4
openai~=2.49

# Speech (Demo 4)
azure-cognitiveservices-speech~=1.51

# Content Understanding (Demo 6) is called over REST
requests~=2.31

# Azure AI Language — Demo 3 Part C (PII detection & redaction)
azure-ai-textanalytics~=5.3

# OPTIONAL — Module 4 Part C (Voice Live, preview). Not installed by default.
# Uncomment (or `pip install "azure-ai-voicelive[aiohttp]" pyaudio`) to run voice_live_chat.py.
# azure-ai-voicelive[aiohttp]~=1.2
# pyaudio~=0.2.14
